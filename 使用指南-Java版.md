# 🔍 故障检测系统 · 使用指南（Java 版）

> 版本：v1.1（Java）｜ 适用对象：现场实施 / 交付 / 运维人员
> 系统位置：`e:\Desktop\中广核驻场实战手册\故障检测系统\fault-detect-system\`
> 后端技术：Spring Boot 2.7 + Java 11 + SQLite（与原 Python 版功能 1:1 对齐，接口/知识库/打分规则一致）

---

## 目录

1. [系统简介](#一系统简介)
2. [快速开始](#二快速开始)
3. [界面总览](#三界面总览)
4. [故障检测（核心功能）](#四故障检测核心功能)
5. [检测结果解读](#五检测结果解读)
6. [场景库](#六场景库)
7. [状态码知识库](#七状态码知识库)
8. [历史记录](#八历史记录)
9. [新增场景（自定义扩展）](#九新增场景自定义扩展)
10. [诊断逻辑说明](#十诊断逻辑说明)
11. [常见问题 FAQ](#十一常见问题-faq)
12. [系统维护](#十二系统维护)
13. [附录](#十三附录)

---

## 一、系统简介

### 1.1 这是什么系统？

**智能故障检测与诊断系统**——为现场实施/交付人员设计的一键排障工具。只需粘贴一个故障页面 URL，系统自动完成 7 步检测流程，锁定问题根因，并输出可直接汇报的 3 段式诊断报告。

> Java 版为原 Python 版的完整移植：8 个接口、7 步检测引擎、92 场景 × 32 状态码知识库、打分规则均已逐项对比验证一致，功能无缺漏。

### 1.2 核心能力

| 能力 | 说明 |
|------|------|
| **一键诊断** | 输入 URL + 症状描述，自动完成 7 步检测 |
| **完整覆盖** | 内置 **92 个场景** × **32 个状态码** 的知识库 |
| **证据链闭环** | 每一步检测都有实时证据 + 知识库证据 |
| **专业报告** | 3 段式汇报模板（现象→排查→结论），一键复制 |
| **灵活扩展** | 支持随时新增自定义场景（立即生效、持久化保存） |
| **独立部署** | Docker 容器或本地 jar 运行，数据存在本地，无需联网 |

### 1.3 适用场景

- ✅ 央国企驻场实施（如中广核项目）
- ✅ 现场故障快速响应
- ✅ 问题定位与向研发/甲方汇报
- ✅ 实施团队培训演练

---

## 二、快速开始

### 2.1 环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11（推荐）或 Linux/macOS |
| Java | **JDK 11** 及以上（命令行/双击脚本方式需要） |
| Maven | 3.6 及以上（仅首次打包需要；用 Docker 方式无需安装） |
| Docker | Docker Desktop（**推荐方式**，无需安装 Java/Maven） |
| 浏览器 | Chrome / Edge 等现代浏览器 |
| 网络 | 仅首次用 Maven 打包需联网下载依赖；使用系统时只需能访问目标系统内网 |

### 2.2 启动系统（三种方式任选其一）

**方式一：Docker 启动（推荐，无需安装 Java/Maven）**

在项目根目录（`fault-detect-system`）打开 PowerShell 执行：

```powershell
docker compose -f docker-compose-java.yml up -d
```

- 自动构建镜像并启动容器 `fault-detect-system-java`
- 数据持久化到项目根 `data\` 目录（容器停止/删除不丢失）
- Docker Desktop 界面中也能直接看到和管理该容器

**方式二：双击启动（本机有 JDK 11 + Maven）**

```
双击 fault-detect-system\start-java.bat
```

首次运行自动用 Maven 打包 jar（需联网下载依赖），随后 `java -jar` 启动并自动打开浏览器。若本机 Maven 不在 `E:\apache-maven-3.9.16`，需修改脚本内 `set MVN=` 一行。

**方式三：命令行启动**

```bash
cd fault-detect-system
java -jar java-backend\target\fault-detect-system.jar
```

### 2.3 验证启动是否成功

启动后做三件事确认系统正常：

| 检查项 | 方法 | 期望结果 |
|--------|------|---------|
| ① 健康检查 | 浏览器打开 `http://localhost:8000/api/health` | 返回 `{"status":"ok","total_scenarios":92,"total_status_codes":32}` |
| ② 页面加载 | 打开 `http://localhost:8000` | 看到系统主界面，顶部统计显示"场景库：92 个" |
| ③ 场景数量 | 点击顶部"📚 场景库"标签 | 显示"共 92 / 92 个场景"，92 张场景卡片 |

> 常见启动失败原因：8000 端口被占用（见 FAQ Q2）、数据目录无权限（见 FAQ Q10）。

### 2.4 访问系统

| 场景 | 访问地址 |
|------|---------|
| 本机访问 | http://localhost:8000 或 http://127.0.0.1:8000 |
| 局域网访问（同一 WiFi 的手机/其他电脑） | http://本机IP:8000（如 http://192.168.1.213:8000） |
| 获取本机 IP | PowerShell 执行 `ipconfig`，看"IPv4 地址"（如 192.168.x.x） |

> 💡 **停止服务**：Docker 方式执行 `docker compose -f docker-compose-java.yml down`；jar/脚本方式直接关闭启动窗口。

---

## 三、界面总览

系统采用 5 个页签布局：

| 页签 | 功能 | 图标 |
|------|------|------|
| **故障检测** | 核心功能：输入 URL 一键诊断 | 🛠️ |
| **场景库** | 浏览/搜索/筛选 92 个故障场景 | 📚 |
| **状态码** | 32 个 HTTP 状态码速查 | 🔢 |
| **历史记录** | 查看历次检测记录 | 🕘 |
| **新增场景** | 自定义添加场景（自动保存） | ➕ |

顶部统计栏实时显示：场景库数量、状态码数量、自定义场景数量、历史记录条数。每次检测/新增场景后自动刷新。

---

## 四、故障检测（核心功能）

### 4.1 操作步骤

1. 进入 **🛠️ 故障检测** 页签
2. 填写 **故障 URL**（必填）
3. 填写 **症状描述**（建议填写）
4. 选择 **服务存活检测** 开关（默认开启）
5. 选择 **超时时间**（3~30 秒，默认 10 秒）
6. 点击 **🚀 开始检测**

### 4.2 URL 填写规范

| 输入形式 | 示例 | 说明 |
|---------|------|------|
| 完整 URL | `http://192.168.1.100:8081/api/users` | 标准写法，推荐 |
| 不带协议 | `192.168.1.100:8081/api/users` | 自动补 `http://` |
| 域名 | `http://oa.cgnpc.local/report` | 需本机 DNS 可解析 |
| HTTPS | `https://192.168.1.100:8443/api` | 自动忽略证书校验（自签名可测） |
| 带查询参数 | `http://192.168.1.100:8081/api/list?dept=01` | 参数会保留用于探测 |

**关键提醒**：务必填**浏览器地址栏或 F12 Network 中实际出错的请求地址**（红色那条），而不是首页地址。填错地址会测错目标。

### 4.3 症状描述怎么写（影响匹配准确率）

症状描述可贡献最高 18 分匹配分，建议描述"页面表现 + 用户操作"，贴近场景库中的界面症状关键词：

| 症状写法 | 说明 |
|---------|------|
| "页面显示空表格，没有任何数据" | 命中"空表格/无数据"类场景 |
| "点击按钮提示接口不存在" | 命中"接口不存在/404"类场景 |
| "上传文件报文件过大" | 命中"上传限制"类场景 |
| "页面转圈一直打不开" | 命中"超时/无响应"类场景 |

> 💡 **加载示例** 按钮可快速填入演示 URL 熟悉流程；**清空** 按钮重置表单。

### 4.4 检测流程（7 步自动执行）

系统按下述 SOP 自动执行，每一步都有证据：

| 步骤 | 检测项 | 说明 |
|------|--------|------|
| 0 | URL 解析 | 提取协议/主机/端口/路径 |
| 0 | 服务存活检测（TCP） | 模拟 Test-NetConnection 验证端口连通 |
| 1 | HTTP 请求探测 | 实际请求目标，捕获状态码、耗时、响应体 |
| 2 | 状态码分析 | 对照 SOP 状态码速查表给出分析 |
| 3 | 响应数据分析 | 提取响应体特征（JSON 空数组、错误关键词等） |
| 4 | 证据收集 | 自动固定 URL/状态码/耗时/响应体长度 |
| 5 | 分支判断 | 500→日志分析；200+空数组→数据库排查；其他→Postman 隔离验证 |

各状态码在第 5 步的走向：

| 状态码 | 第 5 步提示 |
|--------|------------|
| 500 | 必须查后端日志，定位第一个 Caused by |
| 200 + 空数组 | 必须查数据库（表是否存在/是否有数据） |
| 其他（400/401/404/504…） | Postman 隔离验证：Postman 正常=前端问题；也报错=后端/网络问题 |

### 4.5 典型使用场景示例

**场景 A：页面空表格**

```
URL：   http://192.168.1.100:8081/api/users
症状：  页面显示空表格，没有任何数据
```

预期结果：状态码 200 → 判定"数据库无数据"（数据库问题，置信度 93%），建议"检查数据导入流程或重置测试数据"。

**场景 B：登录被拒**

```
URL：   http://192.168.1.100:8081/api/login
症状：  操作被拒绝，提示未登录
```

预期结果：状态码 401 → 判定"未登录/Token 失效"（认证问题，置信度约 100%）。

**场景 C：接口报 500**

```
URL：   http://192.168.1.100:8081/api/user/delete
症状：  点击删除报服务器内部错误
```

预期结果：状态码 500 → 判定后端代码问题（置信度 83%~95%），步骤 5 提示"必须查后端日志"。

---

## 五、检测结果解读

检测完成后页面从上到下依次展示 6 块内容。建议按顺序查看。

### 5.1 状态横幅

- **大字状态码**：颜色随问题归属变化（蓝=前端、黄=后端、红=网络…）
- **状态描述**：如"200"、"502 模拟（连接被拒绝）"
- **报告编号 + 时间**：`RPT_` 开头，用于向研发/甲方引用
- **问题归属标签**：🎯 如"数据库问题"——先看这个定性结论

### 5.2 检测流程时间线（7 步）

逐条列出每步结果，图标含义：

| 图标 | 含义 |
|------|------|
| ✓ 通过 | 该步检测正常 |
| ✗ 失败 | 该步检测异常（重点看） |
| i 信息 | 分析/提示类信息 |
| ⏭ 跳过 | 用户关闭了该检查项 |

**重点看第 0 步（TCP）和第 1 步（HTTP）的失败原因**——这是定位问题的第一现场。

### 5.3 证据链

按序排列的证据条目，供截图/汇报使用：

| 类型 | 内容 |
|------|------|
| 实时证据（live） | URL 解析、TCP 检测结果、HTTP 探测、响应体截断（前 500 字符） |
| 知识库证据（template） | 该场景对应的标准证据模板（已自动填充实际 URL/主机/端口/状态/耗时） |

**用途**：把证据链截图发到工作群，研发可快速复现；汇报时逐条引用更专业。

### 5.4 诊断结论

- **问题归属**：10 类之一（见附录 B）
- **匹配场景**：场景编号 + 名称（如 SCN_004 数据库无数据）
- **置信度**：百分比 + 进度条（0~100%）
- **备选场景**：次优的 2 个候选（分数从高到低）——**结论不确定时务必点开看**
- **诊断结论**：一句话结论

### 5.5 解决建议

该场景对应的解决方向清单。⚠️ 仅提供方向性建议，具体执行需结合现场实际，必要时联系研发确认。

### 5.6 汇报模板（3 段式）

```
【现象】用户访问 http://192.168.1.100:8081/api/users，页面表现：页面显示空表格（状态码 200，耗时 45ms）
【排查过程】
- URL 解析（解析输入 URL）→ http://192.168.1.100:8081/api/users
- 服务存活检测（TCP）（Test-NetConnection ...）→ TcpTestSucceeded: True
- HTTP 请求探测（GET ...）→ 状态码：200 | 耗时：45ms
- ...
【结论】数据库无数据（归属：数据库问题，置信度 93%）
```

点击 **📋 复制汇报文本** 一键复制，直接粘贴到工作群/汇报邮件/工单。

---

## 六、场景库

### 6.1 浏览场景

- 顶部显示"共 X / 92 个场景"
- 每张卡片显示：**编号**（SCN_xxx）、**名称**、**归属标签**（带颜色）、**状态码标签**
- **点击卡片展开详情**：响应特征关键词、界面症状关键词、解决建议

### 6.2 读懂一张场景卡片

以 SCN_004 数据库无数据为例：

```
SCN_004  数据库无数据                     [数据库问题]
[200]                                    ← 状态码标签：该场景由哪些状态码触发
结论：响应体为空数组/无数据，数据库未导入或表为空
▸ 点击展开：
  响应特征关键词：empty、[]、"data":[]、暂无数据   ← 命中这些→加分
  界面症状关键词：空表格、无数据、空白页           ← 症状描述命中→加分
  解决建议：检查数据导入流程 / 重置测试数据 / 核对数据库连接
```

**用法**：遇到新问题时，先搜索场景库看有没有匹配的卡片，把卡片上的"界面症状关键词"抄进检测页的症状描述框，能显著提高命中率。

### 6.3 搜索

在搜索框输入关键词（场景名/结论/编号），实时过滤。示例：
- 输入"未登录" → 过滤出登录/Token 相关场景
- 输入"504" → 过滤出超时类场景
- 输入"SCN_004" → 精确跳转场景

### 6.4 筛选

| 筛选器 | 说明 |
|--------|------|
| 状态码下拉 | 选择状态码（如 500），只看该状态码对应的场景 |
| 归属下拉 | 选择问题归属（如 数据库问题），只看该归属场景 |

三个条件（搜索 + 两个筛选）可任意叠加使用。

---

## 七、状态码知识库

进入 **🔢 状态码** 页签，展示全部 32 个状态码卡片，每张卡片包含：

| 字段 | 说明 |
|------|------|
| 状态码 | 如 200 / 401 / 500 |
| 名称 | 如 OK / Unauthorized / Internal Server Error |
| 问题归属 | 该状态码常对应的问题类别 |
| 说明 | 状态码含义描述 |
| 速记 | 一句话排障提示（📌 标记） |

**快速记忆法**：

- `2xx`：后端正常，看前端渲染与数据
- `4xx`：客户端问题，重点查参数/路径/权限/Token
- `5xx`：服务端问题，重点查日志/服务状态/数据库
- `pending`/超时：先查网络与服务是否存活

**使用技巧**：检测结果里的状态码与这里的卡片一一对应——拿到状态码后翻到对应卡片，看"速记"行，通常直接就是排障方向。

---

## 八、历史记录

进入 **🕘 历史记录** 页签：

- 每次检测自动保存（URL、状态码、归属、场景、置信度、时间）
- 默认展示最近 30 条，每 30 秒自动刷新
- 支持手动点 **🔄 刷新** 按钮
- 历史数据保存在本地 SQLite 数据库 `data\fault_detect.db`

**用途**：交班/复盘时翻历史，看某台服务器今天报过哪些错、定位到哪些场景。

---

## 九、新增场景（自定义扩展）

遇到知识库没有的新问题时，随时添加。**完整操作示例**（以"短信服务欠费"为例）：

### 9.1 操作步骤

1. 进入 **➕ 新增场景** 页签
2. 填写字段：

| 字段 | 必填 | 本例填写 | 说明 |
|------|------|---------|------|
| 场景名称 | ✅ | 短信服务欠费 | 简洁、可搜索 |
| 状态码 | ✅ | `500, 502` | 触发该场景的 HTTP 状态码（逗号分隔） |
| 问题归属 | ✅ | 后端问题 | 从 10 类中选择 |
| 响应特征关键词 | 建议 | `欠费, balance, insufficient` | 响应体中的关键词（逗号分隔） |
| 界面症状关键词 | 建议 | `发送失败, 短信发不出` | 用户描述的症状关键词（逗号分隔） |
| 结论 | 建议 | 短信服务欠费导致发送失败 | 一句话诊断结论 |
| 解决建议 | 建议 | 联系运营商充值<br/>检查短信余额 | 每行一条 |

3. 点击 **💾 保存场景**，弹出"场景已保存！当前共 93 个场景"

### 9.2 保存后验证（重要）

新增场景保存后，回到 **🛠️ 故障检测** 页，用真实 URL + 对应症状测一次，确认：

- 诊断结论正确指向新场景（SCN_093）
- 置信度合理（命中特征/症状越多越高）

### 9.3 注意事项

- 自定义场景**立即生效**，场景库/检测匹配都会包含它
- 自定义场景会**持久化保存**到 `data\custom_scenarios.json`
- 编号自动递增（SCN_093、SCN_094…），不会覆盖内置场景
- 新增场景在检测匹配中**同等参与打分**
- 想删除自定义场景：编辑 `data\custom_scenarios.json` 删除对应条目 → 重启服务生效

---

## 十、诊断逻辑说明

### 10.1 匹配打分规则

系统按以下权重对 92（+自定义）个场景打分，取最高分者：

| 维度 | 分值 | 说明 |
|------|------|------|
| 状态码精确匹配 | +40 | 最高权重，决定性问题 |
| 状态码接近匹配 | +30 | pending 类场景对 502/504 的宽容匹配 |
| 响应特征命中 | 每项 +8，封顶 24 | 响应体关键词匹配 |
| 症状关键词命中 | 每项 +6，封顶 18 | 用户症状描述匹配 |
| 高优先级加成 | +2 | 场景优先级为"高"时 |
| 概率加权 | ×(0.7+0.3×概率) | 经验概率修正最终分数 |

**置信度** = 分数 / 60（封顶 100%）。

### 10.2 如何获得更准确的诊断？

1. **务必填写症状描述**——可额外贡献最高 18 分
2. 填写**实际出错的请求 URL**（F12 Network 中红色那条），而非首页地址
3. 症状描述贴近场景库中的关键词（先查场景库再填症状）
4. 目标响应慢时调大超时时间，避免误判为 504
5. 多测一次、换不同症状措辞，看结果是否稳定

### 10.3 结果可信度分级

| 置信度 | 处理建议 |
|--------|---------|
| ≥ 80% | 可据此汇报与给出解决方向 |
| 50~80% | 建议查看备选场景，结合证据链人工判断 |
| < 50% | 按"检测流程时间线"逐条人工排查，重点是日志与数据库 |

---

## 十一、常见问题 FAQ

### Q1：双击 start-java.bat 后黑窗口闪退？

- 检查 Java：命令行执行 `java -version`（需 11 及以上）
- 检查 Maven 路径：脚本内 `set MVN=` 需与实际安装位置一致
- 首次打包需联网下载依赖，网络不通会导致打包失败
- 省事方案：直接用 Docker 方式 `docker compose -f docker-compose-java.yml up -d`

### Q2：端口 8000 被占用？

```bash
# 查看占用进程
netstat -ano | findstr :8000
# 若是 Docker 容器占用，先停止
docker compose -f docker-compose-java.yml down
# 若想临时改用其他端口（jar 方式）
java -jar java-backend\target\fault-detect-system.jar --server.port=8088
# 访问 http://localhost:8088
```

### Q3：检测一直提示"请求超时/504"？

1. 先确认目标服务是否启动（看"服务存活检测"步骤是否通过）
2. 调大超时时间（如 30 秒）
3. 用浏览器直接访问该 URL 确认是否可达
4. 检查本机是否能 ping 通目标服务器

### Q4：诊断结果"数据库无数据"但库里有数据？

- 检查后端是否连错数据库实例
- 检查接口是否带了过滤条件（如部门/日期范围）
- 打开"备选场景"查看是否更匹配其他场景

### Q5：能检测 HTTPS / 自签名证书的站点吗？

可以。Java 版探测时自动忽略 SSL 证书校验（内置 trust-all），可正常检测自签名证书站点。

### Q6：检测会不会影响目标系统？

- HTTP 探测为**只读 GET 请求**，不修改数据
- TCP 检测仅为端口连通测试
- 不会注入、不会抓取修改数据

### Q7：系统能访问外网吗？会走代理吗？

- 探测请求**禁用系统代理**（内网地址直连），不受企业代理影响
- 用于现场排查内网系统时，走代理反而会误报

### Q8：历史记录存在哪？会丢失吗？

- 保存在本地 SQLite 数据库 `data\fault_detect.db`（Docker 方式为项目根 `data\` 目录，已挂载持久化）
- 删除该文件即清空历史；复制该文件即可备份
- Docker 容器重建不影响数据（数据在宿主机 `data\`）

### Q9：新增场景后想删除怎么办？

系统暂不提供删除界面（防误删）。编辑 `data\custom_scenarios.json` 删除对应条目，重启服务生效。

### Q10：页面能打开但接口报错 / 数据目录异常？

- 检查 `data\` 目录是否有读写权限（Docker 方式确保宿主机目录存在且有权限）
- 若曾手动设置环境变量 `FAULT_DETECT_DATA_DIR`，确认路径有效
- 查看服务日志（Docker：`docker logs -f fault-detect-system-java`）

### Q11：浏览器打开页面样式乱/无样式？

- 确认访问的是 `http://localhost:8000` 根路径
- 强制刷新（Ctrl+F5）清除缓存
- 确认 `/static/style.css` 可访问（浏览器地址栏输入 http://localhost:8000/static/style.css）

### Q12：场景库只显示 1 个场景卡片？

这是旧版本前端的一个 bug（已修复）。若仍出现：
1. 确认服务是重新打包后的版本（改过前端必须重启服务）
2. 浏览器 **Ctrl+F5** 强制刷新清缓存
3. 检查浏览器控制台（F12）是否有红色报错

### Q13：局域网其他设备访问不了？

- 确认服务监听 `0.0.0.0`（Docker 方式自动映射；jar 方式默认监听所有网卡）
- 放行 Windows 防火墙（需管理员 PowerShell）：
  ```powershell
  netsh advfirewall firewall add rule name="fault-detect-8000" dir=in action=allow protocol=TCP localport=8000
  ```
- 确认手机/电脑与本机在同一 WiFi/网段

---

## 十二、系统维护

### 12.1 目录结构

```
fault-detect-system/
├── start-java.bat              # 一键启动脚本（需 JDK 11 + Maven）
├── docker-compose-java.yml     # Docker 编排（推荐）
├── data/                       # 数据目录（首次运行自动创建）
│   ├── fault_detect.db         # 检测历史（SQLite）
│   └── custom_scenarios.json   # 自定义场景
├── java-backend/               # Java 后端
│   ├── pom.xml                 # Maven 配置（Spring Boot 2.7）
│   ├── Dockerfile              # Docker 镜像构建
│   ├── tools/                  # 辅助脚本
│   │   ├── export_knowledge.py # 知识库导出（Python 数据 → JSON）
│   │   └── compare_py_java.py  # 与 Python 版一致性对比脚本
│   ├── target/
│   │   └── fault-detect-system.jar  # 打包产物（重新部署时使用）
│   └── src/main/
│       ├── java/com/cgn/faultdetect/  # 后端源码（8 个类）
│       └── resources/                 # 配置 + 知识库 JSON + 前端页面
├── frontend/static/            # 前端源文件（改前端改这里）
│   ├── index.html
│   ├── style.css
│   └── app.js
├── backend/                    # 原 Python 版（保留，功能相同）
└── 使用指南-Java版.md           # 本文档
```

> ⚠️ 前端有两份：`frontend/static/`（源）和 `java-backend/src/main/resources/static/`（打包用）。**修改前端时必须两处同步改**（或只改 frontend 后复制到 resources），否则打包出来的界面与源不一致。

### 12.2 Docker 常用命令

| 命令 | 作用 |
|------|------|
| `docker compose -f docker-compose-java.yml up -d` | 启动（后台） |
| `docker compose -f docker-compose-java.yml down` | 停止并删除容器（数据保留） |
| `docker compose -f docker-compose-java.yml up -d --build` | 重新构建镜像并启动（改代码后） |
| `docker logs -f fault-detect-system-java` | 查看运行日志（Ctrl+C 退出） |
| `docker ps` | 查看运行中的容器 |
| `docker restart fault-detect-system-java` | 重启容器 |

### 12.3 重新部署流程（修改代码/前端后）

```powershell
# 1. 重新打包 jar
E:\apache-maven-3.9.16\bin\mvn.cmd -f java-backend\pom.xml clean package -DskipTests

# 2. 重新构建镜像并启动（Docker 方式）
docker compose -f docker-compose-java.yml up -d --build
```

打包成功后在 `java-backend\target\` 生成 `fault-detect-system.jar`。

### 12.4 数据备份与恢复

| 操作 | 方法 |
|------|------|
| 备份历史 | 复制 `data\fault_detect.db` |
| 备份自定义场景 | 复制 `data\custom_scenarios.json` |
| 恢复 | 停止服务 → 覆盖同名文件 → 重启 |
| 清空全部数据 | 停止服务 → 删除 `data` 目录 → 重启（自动重建） |

> Docker 方式：`data\` 位于项目根，直接操作即可，无需进入容器。

### 12.5 环境变量

| 变量 | 作用 | 默认值 |
|------|------|--------|
| `FAULT_DETECT_DATA_DIR` | 数据目录路径 | `data`（jar 所在工作目录下） |

示例（将数据放到其他磁盘，jar 方式）：

```bash
set FAULT_DETECT_DATA_DIR=D:\fault-data
java -jar java-backend\target\fault-detect-system.jar
```

### 12.6 API 接口（供高级用户）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/` | 前端页面 |
| GET | `/api/health` | 健康检查（场景数/状态码数） |
| GET | `/api/status-codes` | 全部状态码 |
| GET | `/api/scenarios?code=500` | 场景列表（可按状态码过滤） |
| POST | `/api/detect` | 执行故障检测 |
| POST | `/api/add-scenario` | 新增场景 |
| GET | `/api/history` | 检测历史 |
| GET | `/api/stats` | 统计信息 |

> ⚠️ Java 版**不提供** Swagger 文档页（原 Python 版 `/docs` 在 Java 版不存在）。

### 12.7 接口调用示例（PowerShell）

```powershell
# 执行检测
$body = '{"url":"http://192.168.1.100:8081/api/users","symptom":"页面显示空表格","enable_service_check":true,"timeout":10}'
Invoke-RestMethod http://localhost:8000/api/detect -Method Post -ContentType "application/json" -Body $body

# 新增场景
$body = '{"name":"短信欠费","http_codes":["500","502"],"root_cause":"backend"}'
Invoke-RestMethod http://localhost:8000/api/add-scenario -Method Post -ContentType "application/json" -Body $body
```

---

## 十三、附录

### A. 32 个状态码清单

| 状态码 | 名称 | 类别 | 快速提示 |
|--------|------|------|---------|
| pending | 无响应/超时 | 网络/服务 | 先查服务存活与网络 |
| 200 | OK | 成功 | 看数据：空数组=数据库问题 |
| 201 | Created | 成功 | 前端是否处理成功响应 |
| 202 | Accepted | 成功 | 异步任务已受理 |
| 204 | No Content | 成功 | 前端是否有提示 |
| 300 | Multiple Choices | 重定向 | 多选一，检查配置 |
| 301 | Moved Permanently | 重定向 | 前端 URL 已变更 |
| 302 | Found | 重定向 | 临时跳转异常 |
| 303 | See Other | 重定向 | 重定向到 GET |
| 304 | Not Modified | 缓存 | 缓存策略问题 |
| 307 | Temporary Redirect | 重定向 | 保持方法重定向 |
| 308 | Permanent Redirect | 重定向 | 永久重定向 |
| 400 | Bad Request | 客户端 | 参数校验失败 |
| 401 | Unauthorized | 认证 | Token 失效/未登录 |
| 402 | Payment Required | 业务 | 付费/额度限制 |
| 403 | Forbidden | 权限 | RBAC 权限不足 |
| 404 | Not Found | 客户端 | 接口/路径不存在 |
| 405 | Method Not Allowed | 客户端 | GET/POST 用错 |
| 406 | Not Acceptable | 客户端 | 响应格式不匹配 |
| 408 | Request Timeout | 网络 | 请求超时 |
| 409 | Conflict | 业务 | 资源冲突/重复数据 |
| 410 | Gone | 客户端 | 资源已删除 |
| 413 | Payload Too Large | 配置 | 上传文件过大 |
| 415 | Unsupported Media Type | 客户端 | Content-Type 错误 |
| 422 | Unprocessable Entity | 客户端 | 字段校验失败 |
| 429 | Too Many Requests | 后端 | 触发限流 |
| 500 | Internal Server Error | 后端 | 代码异常，查日志 |
| 501 | Not Implemented | 后端 | 功能未实现 |
| 502 | Bad Gateway | 服务 | 后端崩溃/网关配置 |
| 503 | Service Unavailable | 服务 | 维护中/过载 |
| 504 | Gateway Timeout | 性能 | 慢 SQL/网关超时 |
| 505 | HTTP Version Not Supported | 网络 | 升级浏览器 |

### B. 10 类问题归属

| 归属 Key | 中文标签 | 典型问题 |
|----------|---------|---------|
| frontend | 前端问题 | 路径写错、字段名不匹配、未处理成功响应 |
| backend | 后端问题 | 代码异常、SQL 错误、逻辑缺陷 |
| database | 数据库问题 | 无数据、表不存在、数据不一致 |
| service | 服务问题 | 服务未启动、进程崩溃、端口未监听 |
| network | 网络问题 | 连接失败、DNS、防火墙、代理 |
| permission | 权限问题 | RBAC 配置、角色权限不足 |
| performance | 性能问题 | 慢 SQL、接口超时、内存不足 |
| config | 配置问题 | 上传大小限制、网关配置、环境配置 |
| auth | 认证问题 | 未登录、Token 失效 |
| business | 业务问题 | 业务流程、数据约束冲突 |

### C. 92 个场景分布

| 状态码 | 场景数 | 状态码 | 场景数 | 状态码 | 场景数 |
|--------|--------|--------|--------|--------|--------|
| pending | 3 | 401 | 4 | 500 | 9 |
| 200 | 10 | 402 | 1 | 501 | 1 |
| 201 | 3 | 403 | 4 | 502 | 3 |
| 202 | 2 | 404 | 4 | 503 | 2 |
| 204 | 2 | 405 | 3 | 504 | 5 |
| 300 | 1 | 406 | 2 | 505 | 1 |
| 301 | 2 | 408 | 2 | | |
| 302 | 3 | 409 | 3 | | |
| 303 | 1 | 410 | 2 | | |
| 304 | 2 | 413 | 3 | | |
| 307 | 1 | 415 | 2 | | |
| 308 | 1 | 422 | 3 | | |
| 400 | 4 | 429 | 3 | | |

### D. 常见状态码 SOP 口诀

```
2xx 正常看数据，3xx 重定向查 URL
400 参数 401 登录，403 权限 404 路径
405 方法 408 慢，413 太大 429 频
500 日志必须查，502 崩溃 503 维护
504 超时慢 SQL，pending 先查服务和网络
```

---

## 结语

本系统覆盖 92 个现场常见故障场景，配合标准 3 段式汇报模板，可显著提升现场排障效率和汇报专业度。Java 版与原 Python 版功能完全对齐，提供 Docker 与本地 jar 两种运行方式，部署更简单（无需 Python 环境）。

**使用建议**：
1. 现场排查时先让系统跑一遍，拿到证据链再人工确认
2. 遇到知识库未覆盖的新问题，随时"新增场景"沉淀为团队资产
3. 推荐用 Docker 方式部署（`docker compose -f docker-compose-java.yml up -d`）
4. 修改前端后记得两处文件同步 + 重新打包 + 重启服务

祝排障顺利！🚀
