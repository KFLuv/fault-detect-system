# 🔍 故障检测系统 - 开发进度记录

> 更新时间：2026-08-26（完成联调）
> 项目路径：`e:\Desktop\中广核驻场实战手册\故障检测系统\fault-detect-system\`

---

## ✅ 已完成（全部）

### 1. 需求与设计
- 已阅读三个附件：《故障排查练习系统》（11 场景）、[《实施助理版 - 故障排查练习指南》](file:///e:/Desktop/故障排查练习系统/实施助理版%20-%20故障排查练习指南.md)、[《功能排障标准 SOP（整合版）》](file:///e:/Desktop/中广核驻场实战手册/功能排障标准%20SOP（整合版）.md)
- 方案：**完整前后端系统**（FastAPI 后端 + SQLite + 前端页面），32 状态码 × 92 场景

### 2. 代码交付清单
| 文件 | 说明 |
|------|------|
| `backend/requirements.txt` | fastapi + uvicorn + requests |
| `backend/knowledge/status_codes.py` | 32 个状态码（含名称/类别/问题归属/速记） |
| `backend/knowledge/scenarios.py` | 92 个场景（含断言），`CATEGORY_LABELS/COLORS` 归属配色 |
| `backend/engine/detector.py` | 7 步检测：URL 解析 → TCP 存活 → HTTP 探测 → 状态码分析 → 响应特征提取 → 证据收集 → Postman/日志分支（已禁用系统代理，内网直连） |
| `backend/engine/matcher.py` | 场景打分匹配 + 证据链构建（安全渲染占位符）+ 3 段式汇报 |
| `backend/main.py` | FastAPI 接口 + SQLite 历史（支持 `FAULT_DETECT_DATA_DIR` 覆盖数据目录） |
| `backend/verify.py` | 离线验证脚本（28 项全过，回归可用） |
| `frontend/static/index.html` | 5 页签界面（故障检测/场景库/状态码/历史记录/新增场景） |
| `frontend/static/style.css` | 深色专业诊断工具风格 + 归属配色体系 |
| `frontend/static/app.js` | 全部接口对接 + 渲染逻辑 |
| `start.bat` | 一键启动（自动建 venv、装依赖、开浏览器） |

### 3. 测试验证结果（2026-08-26）
- ✅ 离线验证 `verify.py`：**28/28 通过**（92 场景/32 状态码/字段完整/全码覆盖/抽样匹配/报告构建/URL 解析）
- ✅ API 冒烟：status-codes=32、scenarios=92、add-scenario、history、stats、?code=401 过滤全部正常
- ✅ 检测引擎三类实测：
  - 200（健康接口）→ 数据库问题/数据库无数据 83%
  - 不可达端口 → 502 模拟/网络问题 79%
  - 404 → 前端问题/前端路径错误 70%
- ✅ 浏览器端到端（browser_use）：页面加载/统计栏/5 页签/检测流程（200→SCN_004 93%）/场景筛选（401→4 个）/状态码 32 卡片/历史记录 10 条/新增场景（94 个）/场景搜索/**控制台零 JS 错误**

### 4. 期间修复的关键 Bug
| Bug | 修复 |
|-----|------|
| matcher 用 `.format()` 渲染证据 → 与 JSON 花括号 `{code}` 冲突 KeyError | 改 `_render_evidence()` 安全替换占位符 |
| `async def detect` 事件循环内阻塞探测 → 自探测死锁超时 | 改为同步 `def`（FastAPI 线程池执行） |
| `trust_env` 误当 request 参数 → TypeError | 改用 `Session().trust_env=False` |
| 场景 SCN_010 "undefined" 症状匹配失败 | 关键词细化 |
| 非法 URL（`http://` 空主机名）未拦截 → 误出报告 | parse_url 增加空主机名校验（返回 400） |

### 5. 二次回归检查（2026-08-26，全部通过）
- ✅ 离线验证 `verify.py` 升级为 **30/30**（新增 2 个无主机名用例）
- ✅ API 边界：空 URL/非法 URL/超长 URL(5000) 全部正确；新增场景缺名称/缺状态码/非法归属均 400
- ✅ 并发测试：**5 个检测同时执行全部成功**（均 200/83%），服务稳定
- ✅ 浏览器完整走查：加载约 0.5-1s；5 页签切换无白屏；检测+复制汇报正常；搜索/筛选即时生效；400px 窄屏无横向溢出（响应式 CSS 生效）；**控制台 0 应用错误**；应用资源全部 200
- ⚠️ 唯一"404"是预览环境注入的 `@vite/client`（HMR 脚本，try/catch 静默处理），非应用问题

---

## 🔧 运行方式（正式环境，无沙箱）

```bat
:: 方式一：双击（推荐）
fault-detect-system\start.bat

:: 方式二：命令行
cd fault-detect-system\backend
..\.venv\Scripts\python.exe -m uvicorn main:app --host 0.0.0.0 --port 8000
:: 浏览器访问 http://localhost:8000
```

- 数据目录：默认 `backend/data/`（首次运行自动建库），可用 `FAULT_DETECT_DATA_DIR` 覆盖
- 自定义场景持久化到数据目录 `custom_scenarios.json`

---

## ⚠️ 注意事项
- **独立工作区**：本系统绝不放入招聘系统（人力招聘全流程数字化归档管理系统）目录内
- 用户偏好：不覆盖已有代码文件；保留原「自动化故障检测系统.html」等旧文件
- 场景断言：`assert len(S) == 92`（scenarios.py 底部）
- 沙箱测试遗留：`.venv\.testdata\` 为测试期数据目录（含 2 个测试场景/历史记录），不影响正式环境，可随时删除
- 沙箱限制：终端只能写招聘系统工作区，`中广核` 目录仅 `.venv` 可写（沙箱测试时数据目录需指到 `.venv\.testdata`）
