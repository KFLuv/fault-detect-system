# 🔍 故障检测系统 · 使用指南

> 版本：v1.0 ｜ 适用对象：现场实施 / 交付 / 运维人员
> 系统位置：`e:\Desktop\中广核驻场实战手册\故障检测系统\fault-detect-system\`

---

## 目录

1. [系统简介](#一系统简介)
2. [快速开始](#二快速开始)
3. [界面总览](#三界面总览)
4. [故障检测（核心功能）](#四故障检测核心功能)
5. [检测结果解读](#五检测结果解读)
6. [场景库](#六场景库)
7. [状态码知识库](#七状态码知识库)
8. [历史记录](#八历史记录)
9. [新增场景（自定义扩展）](#九新增场景自定义扩展)
10. [诊断逻辑说明](#十诊断逻辑说明)
11. [常见问题 FAQ](#十一常见问题-faq)
12. [系统维护](#十二系统维护)
13. [附录](#十三附录)

---

## 一、系统简介

### 1.1 这是什么系统？

**智能故障检测与诊断系统**——为现场实施/交付人员设计的一键排障工具。只需粘贴一个故障页面 URL，系统自动完成 7 步检测流程，锁定问题根因，并输出可直接汇报的 3 段式诊断报告。

### 1.2 核心能力

| 能力 | 说明 |
|------|------|
| **一键诊断** | 输入 URL + 症状描述，自动完成 7 步检测 |
| **完整覆盖** | 内置 **92 个场景** × **32 个状态码** 的知识库 |
| **证据链闭环** | 每一步检测都有实时证据 + 知识库证据 |
| **专业报告** | 3 段式汇报模板（现象→排查→结论），一键复制 |
| **灵活扩展** | 支持随时新增自定义场景与状态码 |
| **独立部署** | 本地运行，数据存在本地，无需联网 |

### 1.3 适用场景

- ✅ 央国企驻场实施（如中广核项目）
- ✅ 现场故障快速响应
- ✅ 问题定位与向研发/甲方汇报
- ✅ 实施团队培训演练

---

## 二、快速开始

### 2.1 环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11（推荐）或 Linux/macOS |
| Python | 3.8 及以上（首次启动需联网安装依赖） |
| 浏览器 | Chrome / Edge 等现代浏览器 |
| 网络 | 仅首次安装依赖需要联网；使用系统时只需能访问目标系统内网 |

### 2.2 启动系统

**方式一：双击启动（推荐）**

```
双击 fault-detect-system\start.bat
```

首次运行会自动完成：
1. 创建虚拟环境 `.venv`
2. 安装依赖（fastapi、uvicorn、requests）
3. 启动后端服务并自动打开浏览器

**方式二：命令行启动**

```bash
cd fault-detect-system\backend
..\.venv\Scripts\python.exe -m uvicorn main:app --host 0.0.0.0 --port 8000
```

### 2.3 访问系统

浏览器打开：**http://localhost:8000**

看到以下界面即表示启动成功：

```
┌─────────────────────────────────────────────────────────┐
│  🔍 故障检测系统                                        │
│  输入 URL 一键诊断 · 32 状态码 × 92 场景 · 证据链闭环    │
│  📚 场景库：92 个  🔢 状态码：32 个  🕘 历史记录：0 条   │
├─────────────────────────────────────────────────────────┤
│  🛠️ 故障检测 | 📚 场景库(92) | 🔢 状态码(32) |          │
│  🕘 历史记录 | ➕ 新增场景                               │
└─────────────────────────────────────────────────────────┘
```

> 💡 **停止服务**：关闭启动命令的终端窗口即可。

---

## 三、界面总览

系统采用 5 个页签布局：

| 页签 | 功能 | 图标 |
|------|------|------|
| **故障检测** | 核心功能：输入 URL 一键诊断 | 🛠️ |
| **场景库** | 浏览/搜索/筛选 92 个故障场景 | 📚 |
| **状态码** | 32 个 HTTP 状态码速查 | 🔢 |
| **历史记录** | 查看历次检测记录 | 🕘 |
| **新增场景** | 自定义添加场景（自动保存） | ➕ |

顶部统计栏实时显示：场景库数量、状态码数量、自定义场景数量、历史记录条数。

---

## 四、故障检测（核心功能）

### 4.1 操作步骤

1. 进入 **🛠️ 故障检测** 页签
2. 填写 **故障 URL**（必填）
   - 支持完整 URL：`http://192.168.1.100:8081/api/users`
   - 支持 IP 域名：`192.168.1.100:8081/api/users`（自动补 http://）
   - 注意：需填写**浏览器地址栏或 F12 Network 中实际出错的请求地址**
3. 填写 **症状描述**（建议填写，可显著提高匹配准确率）
   - 示例："页面显示空表格，没有任何数据"
   - 示例："点击按钮提示接口不存在"
   - 示例："上传文件报文件过大"
4. 选择 **服务存活检测** 开关（默认开启）
   - 开启：先做 TCP 端口连通测试，快速定位"服务未启动"类问题
5. 选择 **超时时间**（3~30 秒，默认 10 秒）
   - 探测目标较慢时可调大，避免误判为超时
6. 点击 **🚀 开始检测**

> 💡 **加载示例** 按钮可快速填入一个演示 URL 熟悉流程；**清空** 按钮重置表单。

### 4.2 检测流程（7 步自动执行）

系统按下述 SOP 自动执行，每一步都有证据：

| 步骤 | 检测项 | 说明 |
|------|--------|------|
| 0 | URL 解析 | 提取协议/主机/端口/路径 |
| 0 | 服务存活检测（TCP） | 模拟 Test-NetConnection 验证端口连通 |
| 1 | HTTP 请求探测 | 实际请求目标，捕获状态码、耗时、响应体 |
| 2 | 状态码分析 | 对照 SOP 状态码速查表给出分析 |
| 3 | 响应数据分析 | 提取响应体特征（JSON 空数组、错误关键词等） |
| 4 | 证据收集 | 自动固定 URL/状态码/耗时/响应体长度 |
| 5 | 分支判断 | 500→日志分析；200+空数组→数据库排查；其他→Postman 隔离验证 |

### 4.3 典型使用场景示例

**场景 A：页面空表格**

```
URL：   http://192.168.1.100:8081/api/users
症状：  页面显示空表格，没有任何数据
```

预期结果：状态码 200 → 判定"数据库无数据"（数据库问题，置信度 93%），给出建议"检查数据导入流程或重置测试数据"。

**场景 B：登录被拒**

```
URL：   http://192.168.1.100:8081/api/login
症状：  操作被拒绝，提示未登录
```

预期结果：状态码 401 → 判定"未登录/Token 失效"（认证问题，置信度约 100%）。

**场景 C：接口报 500**

```
URL：   http://192.168.1.100:8081/api/user/delete
症状：  点击删除报服务器内部错误
```

预期结果：状态码 500 → 判定后端代码问题（置信度 83%~95%），步骤 5 提示"必须查后端日志"，证据含 SQL/空指针等特征分析。

---

## 五、检测结果解读

检测完成后页面依次展示 6 块内容：

### 5.1 状态横幅

- 大字显示状态码（颜色随问题归属变化）
- 显示报告编号（RPT_ 开头）、检测时间
- 显示问题归属标签（🎯 如"数据库问题"）

### 5.2 检测流程时间线

逐条列出 7 步执行结果，每步有：
- 标题（如"服务存活检测（TCP）"）
- 执行动作（如"Test-NetConnection 192.168.1.100 -Port 8081"）
- 结果明细（✓ 通过 / ✗ 失败 / ℹ 信息）

### 5.3 证据链

按序排列的证据条目，供截图/汇报使用：

| 类型 | 内容 |
|------|------|
| 实时证据（live） | URL 解析、TCP 检测结果、HTTP 探测、响应体截断 |
| 知识库证据（template） | 该场景对应的标准证据模板（已自动填充实际值） |

### 5.4 诊断结论

- **问题归属**：10 类之一（见附录 B）
- **匹配场景**：场景编号 + 名称（如 SCN_004 数据库无数据）
- **置信度**：百分比 + 进度条
- **备选场景**：次优的 2 个候选（分数从高到低）
- **诊断结论**：一句话结论

### 5.5 解决建议

该场景对应的解决方向清单（如"检查数据导入流程或重置测试数据"）。⚠️ 仅提供方向性建议，具体执行需结合现场实际。

### 5.6 汇报模板（3 段式）

```
【现象】用户访问 http://192.168.1.100:8081/api/users，页面表现：页面显示空表格（状态码 200，耗时 45ms）
【排查过程】
- URL 解析（解析输入 URL）→ http://192.168.1.100:8081/api/users
- 服务存活检测（TCP）（Test-NetConnection ...）→ TcpTestSucceeded: True
- HTTP 请求探测（GET ...）→ 状态码：200 | 耗时：45ms
- ...
【结论】数据库无数据（归属：数据库问题，置信度 93%）
```

点击 **📋 复制汇报文本** 可一键复制，直接粘贴到工作群/汇报邮件/工单。

---

## 六、场景库

进入 **📚 场景库** 页签：

### 6.1 浏览

- 顶部显示"共 X / 92 个场景"
- 场景卡片显示：编号（SCN_xxx）、名称、归属标签（带颜色）、状态码标签
- 点击卡片可**展开详情**：响应特征关键词、界面症状关键词、解决建议

### 6.2 搜索

在搜索框输入关键词（场景名/结论/编号），实时过滤。示例：
- 输入"未登录" → 过滤出登录/Token 相关场景
- 输入"504" → 过滤出超时类场景

### 6.3 筛选

| 筛选器 | 说明 |
|--------|------|
| 状态码下拉 | 选择状态码（如 500），只看该状态码对应的场景 |
| 归属下拉 | 选择问题归属（如 数据库问题），只看该归属场景 |

两个筛选可叠加，也可与搜索组合使用。

---

## 七、状态码知识库

进入 **🔢 状态码** 页签，展示全部 32 个状态码卡片，每张卡片包含：

| 字段 | 说明 |
|------|------|
| 状态码 | 如 200 / 401 / 500 |
| 名称 | 如 OK / Unauthorized / Internal Server Error |
| 问题归属 | 该状态码常对应的问题类别 |
| 说明 | 状态码含义描述 |
| 速记 | 一句话排障提示（📌 标记） |

**快速记忆法**：

- `2xx`：后端正常，看前端渲染与数据
- `4xx`：客户端问题，重点查参数/路径/权限/Token
- `5xx`：服务端问题，重点查日志/服务状态/数据库
- `pending`/超时：先查网络与服务是否存活

---

## 八、历史记录

进入 **🕘 历史记录** 页签：

- 每次检测自动保存（URL、状态码、归属、场景、置信度、时间）
- 默认展示最近 30 条，每 30 秒自动刷新
- 支持手动点 **刷新** 按钮
- 历史数据保存在本地数据库（见第 12 章）

---

## 九、新增场景（自定义扩展）

遇到知识库没有的新问题时，随时添加：

### 9.1 操作步骤

1. 进入 **➕ 新增场景** 页签
2. 填写字段：

| 字段 | 必填 | 说明 |
|------|------|------|
| 场景名称 | ✅ | 如"新版本导出 Excel 乱码" |
| 状态码 | ✅ | 触发该场景的 HTTP 状态码（可多个，逗号分隔） |
| 问题归属 | ✅ | 从 10 类中选择 |
| 响应特征关键词 | 建议 | 响应体中的关键词（逗号分隔），如 `乱码, charset` |
| 界面症状关键词 | 建议 | 用户描述的症状关键词（逗号分隔），如 `导出乱码` |
| 结论 | 建议 | 一句话诊断结论，如"后端导出编码配置错误" |
| 解决建议 | 建议 | 每行一条解决方向 |

3. 点击 **💾 保存场景**，弹出"场景已保存！当前共 X 个场景"

### 9.2 注意事项

- 自定义场景**立即生效**，场景库/检测匹配都会包含它
- 自定义场景会**持久化保存**到数据目录 `custom_scenarios.json`
- 编号自动递增（SCN_093、SCN_094…），不会覆盖内置场景
- 新增场景在检测匹配中**同等参与打分**

---

## 十、诊断逻辑说明

### 10.1 匹配打分规则

系统按以下权重对 92（+自定义）个场景打分，取最高分者：

| 维度 | 分值 | 说明 |
|------|------|------|
| 状态码精确匹配 | +40 | 最高权重，决定性问题 |
| 状态码接近匹配 | +30 | pending 类场景对 502/504 的宽容匹配 |
| 响应特征命中 | 每项 +8，封顶 24 | 响应体关键词匹配 |
| 症状关键词命中 | 每项 +6，封顶 18 | 用户症状描述匹配 |
| 高优先级加成 | +2 | 场景优先级为"高"时 |
| 概率加权 | ×(0.7+0.3×概率) | 经验概率修正最终分数 |

**置信度** = 分数 / 60（封顶 100%）。

### 10.2 如何获得更准确的诊断？

1. **务必填写症状描述**——可额外贡献最高 18 分
2. 填写**实际出错的请求 URL**（F12 Network 中红色那条），而非首页地址
3. 症状描述贴近场景库中的关键词（见场景库详情）
4. 目标响应慢时调大超时时间，避免误判为 504

### 10.3 结果可信度

- **置信度 ≥ 80%**：可据此汇报与给出解决方向
- **置信度 50~80%**：建议查看备选场景，结合证据链人工判断
- **置信度 < 50%**：按"检测流程时间线"逐条人工排查，重点是日志与数据库

---

## 十一、常见问题 FAQ

### Q1：双击 start.bat 后黑窗口闪退？

- 检查 Python 是否安装：命令行执行 `python --version`
- 查看是否提示"依赖安装失败"——首次启动需联网
- 手动方式：打开 start.bat 所在目录的 `backend` 文件夹，运行 `..\.venv\Scripts\python.exe -m uvicorn main:app --port 8000` 看报错信息

### Q2：端口 8000 被占用？

```bash
# 查看占用
netstat -ano | findstr :8000
# 修改端口：改用其他端口启动
..\.venv\Scripts\python.exe -m uvicorn main:app --port 8088
# 浏览器访问 http://localhost:8088
```

### Q3：检测一直提示"请求超时/504"？

1. 先确认目标服务是否启动（看"服务存活检测"步骤是否通过）
2. 调大超时时间（如 30 秒）
3. 用浏览器直接访问该 URL 确认是否可达
4. 检查本机是否能 ping 通目标服务器

### Q4：诊断结果"数据库无数据"但库里有数据？

- 检查后端是否连错数据库实例
- 检查接口是否带了过滤条件（如部门/日期范围）
- 打开"备选场景"查看是否更匹配其他场景

### Q5：能检测 HTTPS / 自签名证书的站点吗？

可以。系统探测时自动忽略 SSL 证书校验（`verify=False`），可正常检测自签名证书站点。

### Q6：检测会不会影响目标系统？

- HTTP 探测为**只读 GET 请求**，不修改数据
- TCP 检测仅为端口连通测试
- 不会注入、不会抓取修改数据

### Q7：系统能访问外网吗？会走代理吗？

- 探测请求**禁用系统代理**（`trust_env=False`），内网地址直连
- 用于现场排查内网系统，不受企业代理影响

### Q8：历史记录存在哪？会丢失吗？

- 保存在本地 SQLite 数据库（`backend\data\fault_detect.db`）
- 删除该文件即清空历史；复制该文件即可备份

### Q9：新增场景后想删除怎么办？

系统暂不提供删除界面（防误删）。可直接编辑数据目录下的 `custom_scenarios.json` 手动删除对应条目，重启服务生效。

### Q10：浏览器打开页面样式乱/无样式？

- 确认访问的是 `http://localhost:8000` 根路径
- 强制刷新（Ctrl+F5）清除缓存
- 确认 `/static/style.css` 可访问（浏览器地址栏输入 http://localhost:8000/static/style.css）

---

## 十二、系统维护

### 12.1 目录结构

```
fault-detect-system/
├── start.bat                  # 一键启动脚本
├── backend/                   # 后端
│   ├── main.py                # FastAPI 服务入口
│   ├── requirements.txt       # 依赖清单
│   ├── verify.py              # 离线回归测试（30 项）
│   ├── knowledge/             # 知识库
│   │   ├── status_codes.py    # 32 个状态码
│   │   └── scenarios.py       # 92 个场景 + 归属分类
│   ├── engine/                # 检测引擎
│   │   ├── detector.py        # 7 步检测流程
│   │   └── matcher.py         # 场景匹配 + 报告生成
│   └── data/                  # 数据目录（首次运行自动创建）
│       ├── fault_detect.db    # 检测历史（SQLite）
│       └── custom_scenarios.json  # 自定义场景
├── frontend/static/           # 前端
│   ├── index.html
│   ├── style.css
│   └── app.js
└── 使用指南.md                 # 本文档
```

### 12.2 回归测试

代码修改后建议运行离线验证：

```bash
cd backend
..\.venv\Scripts\python.exe -B verify.py
```

预期输出：**通过 30 项，失败 0 项**（覆盖知识库完整性、匹配正确性、报告构建、URL 解析边界）。

### 12.3 数据备份与恢复

| 操作 | 方法 |
|------|------|
| 备份历史 | 复制 `backend\data\fault_detect.db` |
| 备份自定义场景 | 复制 `backend\data\custom_scenarios.json` |
| 恢复 | 停止服务 → 覆盖同名文件 → 重启 |
| 清空全部数据 | 停止服务 → 删除 `backend\data` 目录 → 重启（自动重建） |

### 12.4 环境变量

| 变量 | 作用 | 默认值 |
|------|------|--------|
| `FAULT_DETECT_DATA_DIR` | 数据目录路径 | `backend\data` |

示例（将数据放到其他磁盘）：

```bash
set FAULT_DETECT_DATA_DIR=D:\fault-data
..\.venv\Scripts\python.exe -m uvicorn main:app --port 8000
```

### 12.5 API 接口（供高级用户）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/` | 前端页面 |
| GET | `/api/health` | 健康检查（场景数/状态码数） |
| GET | `/api/status-codes` | 全部状态码 |
| GET | `/api/scenarios?code=500` | 场景列表（可按状态码过滤） |
| POST | `/api/detect` | 执行故障检测 |
| POST | `/api/add-scenario` | 新增场景 |
| GET | `/api/history` | 检测历史 |
| GET | `/api/stats` | 统计信息 |

接口文档（Swagger）：启动后访问 **http://localhost:8000/docs**

---

## 十三、附录

### A. 32 个状态码清单

| 状态码 | 名称 | 类别 | 快速提示 |
|--------|------|------|---------|
| pending | 无响应/超时 | 网络/服务 | 先查服务存活与网络 |
| 200 | OK | 成功 | 看数据：空数组=数据库问题 |
| 201 | Created | 成功 | 前端是否处理成功响应 |
| 202 | Accepted | 成功 | 异步任务已受理 |
| 204 | No Content | 成功 | 前端是否有提示 |
| 300 | Multiple Choices | 重定向 | 多选一，检查配置 |
| 301 | Moved Permanently | 重定向 | 前端 URL 已变更 |
| 302 | Found | 重定向 | 临时跳转异常 |
| 303 | See Other | 重定向 | 重定向到 GET |
| 304 | Not Modified | 缓存 | 缓存策略问题 |
| 307 | Temporary Redirect | 重定向 | 保持方法重定向 |
| 308 | Permanent Redirect | 重定向 | 永久重定向 |
| 400 | Bad Request | 客户端 | 参数校验失败 |
| 401 | Unauthorized | 认证 | Token 失效/未登录 |
| 402 | Payment Required | 业务 | 付费/额度限制 |
| 403 | Forbidden | 权限 | RBAC 权限不足 |
| 404 | Not Found | 客户端 | 接口/路径不存在 |
| 405 | Method Not Allowed | 客户端 | GET/POST 用错 |
| 406 | Not Acceptable | 客户端 | 响应格式不匹配 |
| 408 | Request Timeout | 网络 | 请求超时 |
| 409 | Conflict | 业务 | 资源冲突/重复数据 |
| 410 | Gone | 客户端 | 资源已删除 |
| 413 | Payload Too Large | 配置 | 上传文件过大 |
| 415 | Unsupported Media Type | 客户端 | Content-Type 错误 |
| 422 | Unprocessable Entity | 客户端 | 字段校验失败 |
| 429 | Too Many Requests | 后端 | 触发限流 |
| 500 | Internal Server Error | 后端 | 代码异常，查日志 |
| 501 | Not Implemented | 后端 | 功能未实现 |
| 502 | Bad Gateway | 服务 | 后端崩溃/网关配置 |
| 503 | Service Unavailable | 服务 | 维护中/过载 |
| 504 | Gateway Timeout | 性能 | 慢 SQL/网关超时 |
| 505 | HTTP Version Not Supported | 网络 | 升级浏览器 |

### B. 10 类问题归属

| 归属 Key | 中文标签 | 典型问题 |
|----------|---------|---------|
| frontend | 前端问题 | 路径写错、字段名不匹配、未处理成功响应 |
| backend | 后端问题 | 代码异常、SQL 错误、逻辑缺陷 |
| database | 数据库问题 | 无数据、表不存在、数据不一致 |
| service | 服务问题 | 服务未启动、进程崩溃、端口未监听 |
| network | 网络问题 | 连接失败、DNS、防火墙、代理 |
| permission | 权限问题 | RBAC 配置、角色权限不足 |
| performance | 性能问题 | 慢 SQL、接口超时、内存不足 |
| config | 配置问题 | 上传大小限制、网关配置、环境配置 |
| auth | 认证问题 | 未登录、Token 失效 |
| business | 业务问题 | 业务流程、数据约束冲突 |

### C. 92 个场景分布

| 状态码 | 场景数 | 状态码 | 场景数 | 状态码 | 场景数 |
|--------|--------|--------|--------|--------|--------|
| pending | 3 | 401 | 4 | 500 | 9 |
| 200 | 10 | 402 | 1 | 501 | 1 |
| 201 | 3 | 403 | 4 | 502 | 3 |
| 202 | 2 | 404 | 4 | 503 | 2 |
| 204 | 2 | 405 | 3 | 504 | 5 |
| 300 | 1 | 406 | 2 | 505 | 1 |
| 301 | 2 | 408 | 2 | | |
| 302 | 3 | 409 | 3 | | |
| 303 | 1 | 410 | 2 | | |
| 304 | 2 | 413 | 3 | | |
| 307 | 1 | 415 | 2 | | |
| 308 | 1 | 422 | 3 | | |
| 400 | 4 | 429 | 3 | | |

### D. 常见状态码 SOP 口诀

```
2xx 正常看数据，3xx 重定向查 URL
400 参数 401 登录，403 权限 404 路径
405 方法 408 慢，413 太大 429 频
500 日志必须查，502 崩溃 503 维护
504 超时慢 SQL，pending 先查服务和网络
```

---

## 结语

本系统覆盖 92 个现场常见故障场景，配合标准 3 段式汇报模板，可显著提升现场排障效率和汇报专业度。

**使用建议**：
1. 现场排查时先让系统跑一遍，拿到证据链再人工确认
2. 遇到知识库未覆盖的新问题，随时"新增场景"沉淀为团队资产
3. 定期运行 `verify.py` 确保系统健康

祝排障顺利！🚀
