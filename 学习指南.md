# 故障检测系统 · 学习指南（实施实习生版）

> 配套系统：智能故障检测与诊断系统（http://localhost:8000）
> 适用对象：实施/交付实习生
> 学习方式：**边用系统排障，边学手动原理**——系统负责"立刻排出归属"，你负责"看懂它怎么排的"

---

## 0. 怎么用这份指南（学习路径总览）

```
第 1 课：读懂 HTTP 状态码 ── 地基（先看）
第 2 课：7 步排障 SOP ───── 骨架（系统的每一步在干嘛）
第 3 课：故障归属判定心法 ── 核心（怎么一秒归类）
第 4 课：真实案例复盘 ───── 实战（今天刚踩过的坑）
第 5 课：现场汇报 ──────── 变现（把排障变成绩效）
第 6 课：进阶路线 ──────── 方向（实习期怎么成长）
附录：自测题 + 一页纸速查卡
```

**建议节奏**：第一周读 1-3 课 + 每天用系统测 3 个 URL；第二周开始用第 4 课方法独立排障；第三周起每次检测都练"先自己判归属，再对比系统结论"。

---

## 第 1 课：读懂 HTTP 状态码（地基）

> 系统报告第 2 步会给出状态码，第 4 步证据链包含状态码记忆口诀。**状态码就是服务器给你的"一句话诊断"**，看懂它，50% 的故障已经定位。

### 1.1 先记总纲：按数字分"阵营"

| 数字段 | 阵营 | 第一反应 |
|--------|------|---------|
| 2xx | 成功 | 后端没问题，往"数据/前端渲染"想 |
| 3xx | 重定向 | 地址跳了，查 URL 和路由 |
| 4xx | 客户端错误 | 前端请求有问题（参数/路径/凭证） |
| 5xx | 服务端错误 | 后端/服务/数据库出了问题 |

> 记忆口诀：**"2 成 3 转 4 错 5 挂"**——2 成功、3 跳转、4 客户端错、5 服务端挂。

### 1.2 32 个状态码记忆口诀（系统内置，逐条过一遍）

**① 未响应类**

| 状态 | 含义 | 口诀 | 下一步 |
|------|------|------|--------|
| pending | 无响应/超时 | 空白/pending → 网络/服务问题，先做第 0 步 | 第 0 步检查服务存活（TCP 连接测试） |

**② 2xx 成功类（后端正常）**

| 状态 | 含义 | 口诀 | 下一步 |
|------|------|------|--------|
| 200 | OK | 200 → 后端正常，看返回数据 | 分析响应数据（有数据=前端渲染问题；空数组=数据库没数据） |
| 201 | 已创建 | 201 → 创建成功，看前端有无提示 | 检查前端是否处理成功响应 |
| 202 | 已接受 | 202 → 异步任务，查任务进度 | 检查异步任务状态 |
| 204 | 无内容 | 204 → 成功无内容，前端要加提示 | 检查前端是否处理成功响应 |

**③ 3xx 重定向类（地址跳了）**

| 状态 | 含义 | 口诀 | 下一步 |
|------|------|------|--------|
| 300 | 多选项 | 300 → 多选项，检查服务端配置 | 检查服务端多选项配置 |
| 301 | 永久重定向 | 301 → 永久重定向，改前端 URL | 修正前端 URL |
| 302 | 临时重定向 | 302 → 临时重定向，查是否死循环 | 检查重定向配置是否异常 |
| 303 | 看其他 | 303 → POST 后跳转 GET，前端处理 | 检查前端是否跟随重定向 |
| 304 | 未修改 | 304 → 命中缓存，数据不更新查缓存 | 检查缓存策略 |
| 307 | 临时重定向 | 307 → 临时重定向，查路由配置 | 检查重定向目标配置 |
| 308 | 永久重定向 | 308 → 永久重定向，查配置 | 检查重定向规则 |

**④ 4xx 客户端错误类（前端请求的问题）**

| 状态 | 含义 | 口诀 | 下一步 |
|------|------|------|--------|
| 400 | 请求错误 | 400 → 参数错误，前端校验不足 | 检查请求参数（F12 → Payload） |
| 401 | 未授权 | 401 → 未登录/Token 失效，重新登录 | 检查登录状态 / Token |
| 402 | 需付费 | 402 → 付费功能未开通 | 检查业务功能开通状态 |
| 403 | 禁止访问 | 403 → 权限不足，查 RBAC 配置 | 检查角色权限配置 |
| 404 | 未找到 | 404 → 路径错误 or 未开发（需验证） | F12 核对请求路径与后端路由 |
| 405 | 方法不允许 | 405 → GET/POST 用错或未实现 | 核对请求方法与后端接口定义 |
| 406 | 不可接受 | 406 → Accept 头与响应格式不匹配 | 检查请求头 Accept |
| 408 | 请求超时 | 408 → 请求超时，查网络与超时配置 | 检查网络与超时配置 |
| 409 | 冲突 | 409 → 冲突，查约束与重复数据 | 检查唯一约束与重复数据 |
| 410 | 已删除 | 410 → 资源已删除，清理前端调用 | 清理前端对已删资源的调用 |
| 413 | 载荷过大 | 413 → 文件超限，查配置 | 检查上传大小配置 |
| 415 | 类型不支持 | 415 → Content-Type 错误 | 检查请求 Content-Type |
| 422 | 不可处理 | 422 → 参数校验失败 | 对照后端 DTO 校验字段 |
| 429 | 请求过多 | 429 → 限流，等一会或加权限 | 等待限流窗口或提升配额 |

**⑤ 5xx 服务端错误类（后端/服务/数据库）**

| 状态 | 含义 | 口诀 | 下一步 |
|------|------|------|--------|
| 500 | 服务器内部错误 | 500 → 后端问题，查日志 | **查看后端日志堆栈（必做）** |
| 501 | 未实现 | 501 → 功能未实现 | 确认后端是否实现该方法 |
| 502 | 网关错误 | 502 → 后端服务挂了，重启 | 检查后端进程与 Nginx 配置 |
| 503 | 服务不可用 | 503 → 服务维护/过载 | 检查服务状态与过载情况 |
| 504 | 网关超时 | 504 → 后端处理太久，超时了 | 查慢 SQL / 接口性能 / 网关超时配置 |
| 505 | 版本不支持 | 505 → HTTP 版本不支持 | 升级浏览器/检查协议配置 |

### 1.3 状态码速判训练（第一课重点）

拿到任何状态码，先念口诀，再想归属：

```
401/403 → 认证/权限 → 凭证、RBAC
404     → 路径     → F12 对 URL、对路由
400/422 → 参数     → F12 对 Payload、对 DTO
500     → 后端     → 查日志（必做）
502     → 服务     → 进程、Nginx
504     → 性能     → 慢 SQL、超时配置
pending → 网络/服务 → TCP 连通性
200     → 看数据   → 空数据查库、有数据查前端
```

> 这就是"立刻排出归属"的底层原理：**状态码 → 阵营 → 归属**。

---

## 第 2 课：7 步排障 SOP（系统的骨架）

> 系统每次检测固定走 7 步（第 0 步 → 第 5 步）。**每一步你都要知道：它在测什么？为什么这么测？手动做该怎么做？**

### 第 0 步：URL 解析 + 服务存活检测（TCP）

| 项 | 内容 |
|----|------|
| 系统做什么 | 解析 URL（协议/主机/端口/路径）；TCP 连目标端口（3 秒），判断服务活着没 |
| 为什么 | 服务没起，后面全白测——先确认"门开着没" |
| 手动怎么做 | `ping 主机`（网络通不通）+ `telnet 主机 端口`（端口开不开） |
| 读证据 | "TCP 连接成功/失败"——失败直接锁定网络或服务问题 |

### 第 1 步：HTTP 请求探测

| 项 | 内容 |
|----|------|
| 系统做什么 | 用裸请求（无浏览器环境、无登录凭证）GET 目标 URL，拿状态码 + 响应体 |
| 为什么 | 等价 Postman——绕过浏览器，看到**后端真实响应** |
| 手动怎么做 | `curl -i http://地址`（Windows 用 curl.exe） |
| 读证据 | 状态码 + 响应体前 300 字符 + 耗时 |

> 这一步是系统的核心，也是"自动隔离验证"的基础。裸请求能拿到真实状态码，前后端谁的问题立刻分开。

### 第 2 步：状态码分析

| 项 | 内容 |
|----|------|
| 系统做什么 | 对照 SOP 状态码速查表（第 1 课）分析状态码含义 |
| 为什么 | 状态码是第一诊断线索，决定后续方向 |
| 手动怎么做 | 查状态码表（本指南 1.2） |
| 读证据 | 状态码 + 对应口诀 |

### 第 3 步：响应数据分析

| 项 | 内容 |
|----|------|
| 系统做什么 | 解析响应体特征：空数组/提示错误/含特定关键词（token、sql、timeout…） |
| 为什么 | 同样是 200，空数组和正常数据是两种完全不同的故障 |
| 手动怎么做 | 看响应体 JSON：`data` 是否为空、`message` 说什么 |
| 读证据 | 特征列表（empty/token/sql…） |

### 第 4 步：证据收集

| 项 | 内容 |
|----|------|
| 系统做什么 | 固定 3 样证据：接口记录 / 请求面板 / 响应面板 |
| 为什么 | 汇报和提单必须有据可查 |
| 手动怎么做 | F12 → Network → 截图：请求 URL、状态、响应 |

### 第 5 步：归属判定（系统自动完成隔离验证）

| 项 | 内容 |
|----|------|
| 系统做什么 | 按探测结果直接判定归属（见第 3 课判定表） |
| 为什么 | 隔离验证 = 分清前端/后端/网络/数据库的责任 |
| 手动怎么做 | 用 Postman/curl 复现一次，对比浏览器结果 |
| 读证据 | 结论归属 + 置信度 |

> 特别说明：**手动版本第 5 步要开 Postman 复现**（这也是原版 SOP 的要求）；本系统已自动完成这一步（裸请求 = Postman），所以你可以省掉手动操作，直接看结论。

---

## 第 3 课：故障归属判定心法（核心）

> 系统把故障归为 **10 类归属**。学会判定表，你就能在系统出结论前自己先判一遍。

### 3.1 10 类归属判定表

| 归属 | 判定依据（状态码/现象） | 下一步动作 |
|------|------------------------|-----------|
| 🔵 **前端问题** | 200 但页面空白/报错；响应正常数据也在 | 看浏览器控制台 JS 报错、渲染逻辑 |
| 🟢 **后端问题** | 500/501 + 后端日志异常 | 查日志堆栈（必做） |
| 🟡 **数据库问题** | 200 + 空数组；500 + SQL 异常 | 查表是否存在、有没有数据、SQL 对错 |
| 🔴 **服务问题** | 502/503/pending + 端口不通 | 查进程、重启服务 |
| ⚫ **网络问题** | pending/连接拒绝/DNS 失败 | ping/telnet/防火墙/代理 |
| 🟣 **认证问题** | 401/403 | 登录态、Token、RBAC |
| 🟠 **配置问题** | 413/404/502 且服务正常 | 网关、上传限制、路由配置 |
| ⚪ **性能问题** | 504/408/429 | 慢 SQL、超时配置、限流 |
| 🟤 **权限问题** | 403 | 角色权限配置 |
| 🔘 **业务问题** | 业务校验失败/约束冲突 | 业务流程、数据规则 |

### 3.2 一秒归类口诀

```
200 空数据 → 数据库
200 有数据 → 前端
4xx        → 前端参数/凭证（401 认证、404 路径、400 参数）
5xx        → 后端/服务/数据库（500 查日志、502 重启服务、504 查性能）
pending    → 网络/服务
```

### 3.3 判定优先级（系统实际用的逻辑）

1. 先看**连接层**：连不上 → 网络/服务（pending）
2. 再看**状态码**：5xx → 服务端；4xx → 客户端
3. 最后看**数据**：200 + 空 → 数据库；200 + 有数据 → 前端

> 这就是"立刻排出"的完整逻辑链：**连接 → 状态码 → 数据**，三层筛完，归属必出。

---

## 第 4 课：真实案例复盘（今天刚踩过的坑）

> 三个案例都是真实发生的，对照"系统怎么判 + 手动怎么做"完整走一遍。

### 案例 1：浏览器 200，检测 401（伪故障之王）

```
现象：公司日报系统 http://120.196.80.194:8600/api/personal/report/monthly
     浏览器 F12 看 200，系统检测出 401
```

**排查心路**：

| 步骤 | 判断 |
|------|------|
| 看到 401 | 第一反应：认证问题（第 1 课口诀） |
| 但浏览器是 200 | 矛盾！同一 URL 两种结果 → 差异在**请求本身** |
| 找差异 | 浏览器带了登录凭证（Cookie/Token），检测是裸请求没带 |
| 验证 | 系统里填上 Cookie/Token → 重测 → 200 |
| 结论 | **接口正常，是凭证差异**——伪故障 |

**学到的点**：
- 401 ≠ 接口坏了，先问"谁访问的、带没带凭证"
- 检测系统 = 裸请求 = Postman，它是来帮你**复现真实响应**的，不是来给你添麻烦的
- 排查口诀：**浏览器 200 + 检测 401 = 凭证问题，不是故障**

### 案例 2：检测 localhost 报 502（Docker 网络隔离）

```
现象：本地系统 http://localhost:8082/api/announcements
     浏览器 200，检测 502
```

**排查心路**：

| 步骤 | 判断 |
|------|------|
| 看到 502 | 第一反应：服务挂了（口诀） |
| 但浏览器 200 | 服务明明活着 |
| 关键点 | **检测系统跑在 Docker 容器里**——容器里的 localhost 是容器自己，不是你的电脑 |
| 验证 | 容器内访问 192.168.1.213:8082 → 通；访问 localhost:8082 → 拒绝 |
| 结论 | 不是故障，是**网络环境不同**——检测时填 IP 不填 localhost |

**学到的点**：
- Docker 容器有自己的网络命名空间，"localhost"在容器里 ≠ 宿主机
- 测本地服务用 `192.168.x.x` 本机 IP，别用 localhost
- 这也解释了为什么有些系统"部署到 Docker 里就访问不了宿主机数据库"——同一个道理

### 案例 3：目标不可达/超时（网络 or 服务）

```
现象：输入 http://192.168.1.100:8081/api/users（一个不存在的地址）
```

**排查心路**：

| 步骤 | 判断 |
|------|------|
| 第 0 步 TCP | 连接失败 → 网络 or 服务问题锁定 |
| 第 1 步 HTTP | 请求超时/连接拒绝 → pending/504 类 |
| 结论归属 | 网络问题（归属 network）或服务问题 |
| 手动验证 | ping → 不通=网络；通但端口不通=服务没起/防火墙 |

**学到的点**：
- **pending/超时 ≠ 只一种可能**：丢包、防火墙、服务没起、DNS 都可能
- 手动排查顺序：ping（网络）→ telnet 端口（服务）→ 换网络（环境）
- 系统第 0 步的 TCP 检测就是帮你把这个顺序自动化了

### 案例复盘方法论（每次排障后问自己 3 个问题）

1. 系统判的归属是什么？我能不能自己说出**判定依据**（哪个状态码/哪条证据）？
2. 如果让我手动做，对应是哪几步？（查日志？ping？看 Payload？）
3. 这个故障的"口诀"是什么？（下次 3 秒内反应）

---

## 第 5 课：现场汇报（把排障变成绩效）

> 系统输出的报告 = 3 段式 + 证据链，直接对齐现场汇报格式。

### 5.1 3 段式报告怎么用

系统结论卡片的"汇报文本"已经是标准结构：

```
【现象】页面打开后数据为空，浏览器控制台报错
【排查】已用裸请求复现：接口返回 200 但 data 为空数组
【结论】接口正常，数据库查询无数据 → 数据库问题（置信度 98%）
```

**汇报时直接念/复制**，再补一句你的处理动作："已确认表存在但无数据，正在联系后端核对数据初始化"。

### 5.2 证据链截图

- 系统证据链 = 接口记录 + 请求面板 + 响应面板（3 样固定证据）
- 截图要点：**URL + 状态码 + 响应体**三要素同框
- 给研发提单时附上，问题定位时间减半

### 5.3 汇报话术模板（实习生版）

```
接到报障：XX 系统页面报错
我的排查：输入 URL 检测 → 状态码 500 → 查后端日志 → 发现空指针（附日志截图）
我的结论：后端代码空指针异常，已提供证据链，等待研发修复/已临时重启缓解
```

---

## 第 6 课：实习进阶路线

```
阶段 1（第 1-2 周）：会用系统
  每天用系统测 3 个 URL，记录状态码 + 归属，对照第 1 课口诀
  → 达到：看到状态码能立刻说出归属方向

阶段 2（第 3-4 周）：会查证据
  每次检测后，自己先去查日志/ping/看 Payload，再对比系统结论
  → 达到：能解释"为什么判这个归属"

阶段 3（第 5-8 周）：会讲会写
  独立出检测报告 + 3 段式汇报 + 给研发提有效工单
  → 达到：现场能独立接报障

阶段 4（长期）：会预防
  复盘高频故障类型，总结本系统的常见病（401 凭证、502 服务、200 空数据）
  → 达到：从"排障"到"防障"
```

---

## 附录 A：自测题（10 题）

1. 状态码 401 代表什么？第一反应查什么？→（认证问题，查登录态/Token）
2. 200 但页面空表格，优先排查哪层？→（数据库：表/数据）
3. 502 和 504 的区别？→（502 服务挂了连不上；504 连上了但处理超时）
4. 浏览器 200 但检测 401，说明什么？→（凭证差异，接口有认证保护）
5. 检测 localhost 报 502 但浏览器正常，为什么？→（检测在 Docker 容器内，localhost 是容器自己）
6. 400 和 422 都提示参数问题，区别？→（400 参数格式/类型错；422 参数校验失败）
7. 404 一定是路径错吗？→（不一定，还可能是接口未开发，需 F12 验证）
8. pending（无响应）可能是哪三类问题？→（网络丢包/防火墙、服务没起、DNS 失败）
9. 500 状态码的"必做"动作是什么？→（查看后端日志堆栈）
10. 304 是什么意思？数据不更新时查什么？→（命中缓存，查缓存策略）

## 附录 B：一页纸速查卡（打印贴桌上）

```
状态码速判
  200 → 后端正常，看数据（空=查库，有=查前端）
  301/302 → 地址跳了，对 URL
  400/422 → 参数问题，对 Payload
  401 → 未登录/凭证失效
  403 → 权限不足
  404 → 路径错 or 未开发
  429 → 限流
  500 → 后端问题，查日志（必做）
  502 → 服务挂了，重启
  503 → 维护/过载
  504 → 处理超时，查性能
  pending → 网络/服务

归属判定
  连接不通 → 网络/服务
  4xx → 前端请求/凭证
  5xx → 后端/服务/数据库
  200空数据 → 数据库
  200有数据 → 前端

排查顺序
  系统检测 → 归属 → 日志/ping/Payload → 结论 → 汇报

伪故障提醒
  浏览器200+检测401 = 凭证差异（填 Cookie/Token 重测）
  Docker 检测 localhost = 容器网络隔离（改用本机 IP）
```

---

> 学完这套，你从"拿 URL 问系统"进步到"拿 URL 先自己判，再用系统验证"——这就是实施实习生到独立实施的跨越。遇到判不准的归属，随时带着检测报告来问。
